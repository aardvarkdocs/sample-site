The sizing props (`w`, `h`, `miw`, `mih`, `maw`, `mah`) also accept a Mantine spacing token,
but a token there resolves on the *spacing* scale — `md` is `1rem` — so a CSS length is what you
want for a width or a height.
