# AI assistant docs — screenshots

The PNGs in this folder are **placeholders** referenced by `content/ai-assistant.md`.
Replace each one with a real screenshot from the gateway dashboard (or a docs page,
for the reader panel). **Keep the filename** so the Markdown needs no edits.

Recommended capture: ~1280×800, light mode, the managed dashboard at
`gateway.aardvarkdocs.com/dashboard`.

| File | What to capture |
|------|-----------------|
| `ai-assistant-panel.png` | The reader-facing "Ask AI" panel open on a docs page, mid-answer, showing cited sources and the 👍/👎 controls. |
| `insights-overview.png` | The **Insights** tab: the KPI cards (conversations, satisfaction, uncertainty), the answer-quality/verdict bar, and the volume chart. |
| `top-questions.png` | The **Top Questions** clusters — a few theme cards with counts, example questions, and trend chips. |
| `coverage-gaps.png` | The **Coverage Gaps** cards, each with its finding + recommendation and the triage status menu. |
| `conversations.png` | The **Conversations** tab: the filter bar, a few rows with intent labels + custom tags, ideally with a row expanded. |
| `analytics-assistant.png` | The **Ask your analytics** panel with a natural-language question and its answer. |

The placeholders were generated by `scratchpad/gen_placeholders.py` (not committed); you
can delete this README once real screenshots are in.
